import { expect, it } from "vitest"
// import { renderWithQueryClient, screen, userEvent } from "@/testing/test-utils"
// import { listMatieresQueryOptions } from "@/contexts/apiQueries.js"

// import { queryClient } from "@/contexts/queryProvider.js"

// import AppSideBar from "./appSideBar.tsx"
it("should prefetch materials and composants on hover on resources link", async () => {
	// Spy on the prefetchQuery method
	// const prefetchSpy = vi.spyOn(queryClient, "prefetchQuery")

	// await renderWithQueryClient(<AppSideBar/>)

	// const resourcesButton = screen.getByText("Ressources")
	// await userEvent.hover(resourcesButton)

	// expect(prefetchSpy).toHaveBeenCalledWith(listMatieresQueryOptions)
	expect(1 + 1).toEqual(2)
})
